import { NoLinkDirective } from './no-link.directive';

describe('NoLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new NoLinkDirective();
    expect(directive).toBeTruthy();
  });
});
